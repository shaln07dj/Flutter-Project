<manifest xmlns:android="http://schemas.android.com/apk/res/android">
 
    <application
        android:name="${applicationName}"
        android:label="Pauzible" 
       
        android:icon="@mipmap/ic_launcher"> <!-- Change this to your launcher icon -->
    
        <!-- Other application components go here -->

        <!-- android:allowBackup = "false" add this in application tag above to handle 
        backups/shared prefs issue-->

    </application>
    <!-- <queries>
        <package android:name="com.whatsapp" />
    </queries> -->

       <!-- The INTERNET permission is required for development. Specifically,
         the Flutter tool needs it to communicate with the running application
         to allow setting breakpoints, to provide hot reload, etc.
    -->
    <uses-permission android:name="android.permission.INTERNET"/>
</manifest>
